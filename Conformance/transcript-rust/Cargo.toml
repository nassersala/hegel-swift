[package]
name = "transcript-rust"
version = "0.0.0"
edition = "2024"
publish = false

# hegeltest 0.46.1 is the crate that bundles libhegel (hegeltest-c) 0.43.1,
# the engine version every other column loads (Vendor/CHegel.xcframework,
# hegel-go v0.9.5's pin). This column links its own copy of that engine
# statically.
[dependencies]
hegeltest = "=0.46.1"
