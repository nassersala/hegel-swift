// Phase C: the composed system, module `Composed` (the directory keeps the
// name `System`; a Swift module named System shadows Darwin's, which
// Foundation imports, and nothing importing Foundation could then build).
// SystemModel.swift is the relation, Bridge.swift the seam, System.swift
// the code and its harness.
